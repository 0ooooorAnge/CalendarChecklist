pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // 如果下载慢，可以添加阿里云镜像（可选）
        maven { url 'https://maven.aliyun.com/repository/public' }
    }
}

rootProject.name = "CalendarChecklist"
include ':app'